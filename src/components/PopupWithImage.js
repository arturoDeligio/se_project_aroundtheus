import Popup from "./Popup.js";

// export default class PopupWithImage extends Popup {
//     open({link, name}) {
//         this._popupElement.querySelector(".modal__input").textContent = name;
//         find image by modal__image selector, set 'src' and 'alt'
//         super.open()
//     }
// }
